version: 2
jobs:
  build:
    docker:
     - image: circleci/php:7.4-cli
    steps: 
      - checkout 
      - run: echo "composer installing"
      - run: composer install
      - run: CI=true
workflows: 
  version: 2
  build_app:
    jobs:
      - build
